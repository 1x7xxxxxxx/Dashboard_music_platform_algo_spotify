# _archived_retro.md — frozen

REX is now **tool-colocated**: each hook/agent/skill/command/rule carries its own
`rex:` block in its frontmatter. See `.claude/rules/rex-format.md` and the slash
commands `/retro` + `/rex-promote`.

This file is kept only so `/resume` can scan historical `Next session:` /
`Deferred` markers in legacy entries. Do not append new entries here.

---

## [YYYY-MM-DD HH:MM] — Project initialized
**Action taken:** Initial Claude Code configuration deployed via setup-claude-code.sh.
**Next session:** Start capturing REX per-tool — see rex-format.md.
