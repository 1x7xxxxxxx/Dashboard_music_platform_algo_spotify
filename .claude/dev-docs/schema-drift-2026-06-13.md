# Schema drift — prod ↔ canonical, triage 2026-06-13

Generated by `make schema-check` (`tools/dev/schema_drift_check.py`): provisions a
throwaway Postgres from `init_db.sql + migrations/*.sql` (the canonical, version-
controlled schema) and diffs it against the live prod `information_schema`.

**Why it matters:** prod has columns/tables the repo doesn't declare. Code that
reads/writes them works on prod but **500s on a fresh install / in CI** — that is
exactly the `/youtube/videos` bug (prod `youtube_videos.view_count` absent from
canonical). The version-controlled schema must be the single source of truth so a
fresh deploy reproduces prod.

prod: 927 cols / 92 tables · canonical: 903 cols / 90 tables.

## A. USED but undeclared → **reconcile INTO canonical** (real gaps)
Code references these; a fresh install lacks them.

| Drift | Used by | Action |
|---|---|---|
| **table `etl_daily_metrics`** | `views/airflow_kpi.py` | add `CREATE TABLE` to `init_db.sql` + a `*_schema.py` + migration, matching prod's columns (id, dag_id, run_date, total_rows, invalid_rows, anomalies_confirmed, alert_delay_seconds, api_availability_status, created_at) |
| `apple_songs_performance.purchases` | apple view | `ALTER TABLE … ADD COLUMN` in migration + `apple_music_csv_schema.py` + init_db.sql |
| `apple_songs_performance.radio_spins` | apple view | idem |
| `apple_songs_performance.shazam_count` | 5 src files | idem |
| `meta_adsets.age_range` | meta breakdown views | idem in `meta_ads_schema.py` + init_db.sql + migration |

## B. ORPHAN prod-extra → **drop on prod OR document** (unused by current code)
| Drift | Note |
|---|---|
| **table `meta_spotify_mapping`** | 0 code refs — superseded by `campaign_track_mapping`; drop on prod or leave as dead |
| `meta_ads.video_file_name` | 0 refs |
| `meta_adsets.targeting_optimization` | 0 refs |
| `youtube_videos.{view,like,comment}_count` | **orphans** — current code reads counts from `youtube_video_stats` (fixed PR #71). Safe to drop on prod |

## C. prod-MISSING (canonical declares, prod lacks) → low priority
| Drift | Note |
|---|---|
| `youtube_channels.id`, `youtube_videos.id` | prod tables predate the `SERIAL PRIMARY KEY`; they use `video_id`/`channel_id` UNIQUE. Functional, cosmetic only. Reconcile when convenient (add `id` on prod) or align canonical to drop the PK. |

## Durable rule
**Every schema change ships as `migrations/0NN.sql` (+ the matching `*_schema.py` /
`init_db.sql`) — never a manual `ALTER` on prod.** Then prod == init_db.sql +
migrations by construction. Run `make schema-check PROD_SSH=user@host` after any
prod schema touch (and consider a nightly cron on the server) to catch regressions.
Error class: `prod-canonical-schema-drift` (`error-classes.md`).
