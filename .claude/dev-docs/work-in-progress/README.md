# Work In Progress

Each active feature gets a subfolder here with two files:
- `plan.md` — objective, affected files, steps, risks
- `context.md` — technical snapshot to resume after /clear

Create with: `/dev-docs <feature-name>`
Archive to `../archives/` when the brick is complete.
