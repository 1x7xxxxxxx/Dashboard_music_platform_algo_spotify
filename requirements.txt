# --- Data & Processing ---
pandas==2.1.4
psutil>=5.9.0
numpy==1.26.2
xgboost>=2.0.0
# scikit-learn is an implicit runtime dep of xgboost 2.x — see pyproject.toml.
scikit-learn>=1.3.0
shap>=0.44.0

# --- API Clients ---
facebook-business==18.0.0
spotipy==2.23.0
google-api-python-client==2.108.0

# --- Dashboard (Local only - Docker les installera mais ne s'en servira pas) ---
streamlit==1.29.0
plotly==5.18.0
watchdog==3.0.0
xhtml2pdf>=0.2.11

# --- Utilities ---
python-dateutil>=2.8.2
PyYAML==6.0.1
requests==2.31.0
python-dotenv==1.0.0
cryptography>=42.0.0

# --- REST API (Brick 14) ---
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
# Pin bcrypt<4.1: bcrypt 4.1+ rejects passwords >72 bytes with ValueError,
# breaking passlib 1.7.4's detect_wrap_bug probe (passlib initializes its
# bcrypt backend by probing with a 73-byte secret to detect a historical
# wrap bug). Without this pin, the entire bcrypt backend fails to load
# and verify_password / login tests crash. Re-evaluate when passlib >1.7.4
# ships (project is dormant since 2020).
bcrypt>=4.0,<4.1
python-multipart>=0.0.9

# --- Stripe billing (Brick 21) ---
stripe>=8.0.0

# --- TOTP 2FA (Brick 28) ---
pyotp>=2.9.0
qrcode[pil]>=7.4.2

# --- Database Driver ---
psycopg2-binary==2.9.9

# --- Providers Airflow ---
# On ne force PAS la version de Airflow ici, car elle est déjà dans l'image Docker
# On ajoute juste les providers supplémentaires
apache-airflow-providers-postgres
apache-airflow-providers-http
apache-airflow-providers-common-sql
isodate==0.6.1

#Reading xlsx for meta_insight watcher
openpyxl
pandas
psycopg2-binary
python-dotenv