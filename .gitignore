# Python
__pycache__/
*.pyc
*.py[cod]
*.class
*.so
*.egg-info/
.Python
venv/
env/
ENV/

# Streamlit — keep config.toml (infra/security: maxUploadSize, headless, telemetry off),
# only ignore secrets.toml (legacy per-app secrets store)
.streamlit/secrets.toml

#Raccourcis
Raccourcis.txt

# Configuration sensible - CRITIQUE
config/config.yaml
.env
.env.local
.env.*
!.env.example
!.env.railway.example
airflow/.env
**/.env
docker-compose.yml

# Credentials et tokens
.cache
*.token
*.credentials

# Data
data/raw/**/*.csv
data/processed/**/*.csv
*.csv

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs
*.log
logs/

# Jupyter
.ipynb_checkpoints/

# Base de données locale
*.db
*.sqlite

# Airflow
airflow/logs/
airflow/plugins/
airflow/dags/__pycache__/
airflow.db
airflow-webserver.pid

# Machine Learning
machine_learning/mlruns/
machine_learning/data_analysis_ml_perso.ipynb

# Tests et coverage
.pytest_cache/
.coverage
htmlcov/
*.cover

# Graphify code index (machine-local, contains absolute paths)
.mcp.json
graphify-out/

# Ignorer les fichiers de configuration locaux Airflow
airflow.cfg
unittests.cfg
airflow.db
airflow-webserver.pid

# Claude Code session state — never committed
.claude/sessions/
.claude/homunculus/
.claude/scheduled_tasks.lock
.claude/curator/usage.json
.claude/curator/*.tmp

# Local archive — obsolete files kept on disk but excluded from repo
.archive/
backups/

# pip-audit sandbox (make audit-deps) — never committed
.audit-venv/

# schemathesis run cache (make/runbook §6c fuzzing) — never committed
.schemathesis/
data/uploads/

# Copie hors-site (R57) — dépôt de travail et phrase de passe : jamais versionnés.
.offsite-git/
.backup_passphrase
data/offsite_receipt.json

# Le dossier d'architecture est DÉRIVÉ (~1,1 Mo, au-dessus du plafond de 1 Mo).
# Son générateur est versionné sous tools/dev/architecture_dossier/.
#
# Les INTERMÉDIAIRES de sa chaîne le sont aussi, et la raison est la même : le HTML
# rendu (504 Ko), les SVG mermaid et leur source `.mmd` (520 Ko) et le fichier de
# configuration mermaid réécrit à chaque exécution sont tous reconstruits par
# `main.py`. Le 2026-09-10 ils sont entrés dans un commit par un `git add -A` — 1 Mo
# de dérivé qui grossit à chaque régénération, exactement ce que le plafond existe
# pour tenir à distance.
tools/dev/architecture_dossier/dossier.html
tools/dev/architecture_dossier/dossier.pdf
tools/dev/architecture_dossier/mermaid.json
tools/dev/architecture_dossier/svg/
docs/streamlytics-architecture-et-qualite-des-donnees.pdf
